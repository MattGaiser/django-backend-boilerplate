# Django Configuration
DJANGO_ENV=development
DEBUG=true
SECRET_KEY=django-insecure-dev-2%l-xabc_5q%*ch4c+_s4z12)fopd!cl2(h$6t(hj24glyd)nk
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,django

# PostgreSQL Configuration
USE_POSTGRES=true
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Cache Configuration (Development)
USE_REDIS_CACHE=false
REDIS_URL=redis://redis:6379/1
CACHE_KEY_PREFIX=djboiler_dev

# Prefect Configuration
PREFECT_API_URL=http://prefect-server:4200/api
PREFECT_SERVER_HOST=prefect-server
PREFECT_SERVER_PORT=4200
PREFECT_API_DATABASE_CONNECTION_URL=postgresql+asyncpg://postgres:postgres@db:5432/postgres_prefect

# pgAdmin Configuration (for development)
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin

# OAuth2 / SSO Configuration (Development)
# Google OAuth2 - Replace with actual credentials for Google Workspace integration
GOOGLE_OAUTH2_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH2_CLIENT_SECRET=GOCSPX-your-google-client-secret

# Azure AD / Microsoft - Replace with actual credentials for Azure AD integration  
AZURE_AD_CLIENT_ID=your-azure-ad-application-client-id
AZURE_AD_CLIENT_SECRET=your-azure-ad-client-secret

# Google Cloud Storage Configuration (Development)
USE_GCS_EMULATOR=true
GCS_EMULATOR_HOST=http://fake-gcs-server:9090
GCS_BUCKET_NAME=dev-app-assets

# Enterprise Features (Development)
RATELIMIT_ENABLE=true
BACKGROUND_JOBS_ENABLED=true
JOB_RETRY_ATTEMPTS=3
JOB_TIMEOUT=300

# Monitoring & Metrics
PROMETHEUS_EXPORT_MIGRATIONS=false

# Notifications (Development)
NOTIFICATIONS_ENABLED=true
NOTIFICATIONS_EMAIL=true
NOTIFICATIONS_SLACK=false
NOTIFICATIONS_WEBHOOK=false
SLACK_WEBHOOK_URL=
WEBHOOK_ENDPOINTS=

# Webhooks (Development)
WEBHOOKS_ENABLED=false
WEBHOOK_TIMEOUT=30
WEBHOOK_RETRY_ATTEMPTS=3

# Data Export/Import
DATA_EXPORT_ENABLED=true
MAX_EXPORT_RECORDS=10000

# Email Configuration (Development)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=localhost
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@dev.example.com

# Search Configuration
SEARCH_BACKEND=database
ELASTICSEARCH_URL=
SEARCH_INDEX_PREFIX=djboiler_dev

# Feature Flags
FEATURE_FLAGS_CACHE=true
FEATURE_FLAGS_CACHE_TIMEOUT=300

# Version Information (Development)
BUILD_DATE=
GIT_COMMIT=
GIT_BRANCH=development
